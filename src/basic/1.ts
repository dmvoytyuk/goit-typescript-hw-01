const age: number = 50;
const username: string = "Max";
const toggle: boolean = true;
const empty: null = null;

type foo = (a: number) => number;

const callback: foo = function(a) {
	return 100 + a;
};
